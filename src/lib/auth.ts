export async function requireInternalAccess() {
  // TODO(auth): Replace this internal prototype no-op with real authentication
  // and role-based authorization before merging into the production Vercel app.
  return true;
}
